Put real Al Tamam images here once available:
- logo.png / logo-white.png  (company logo, transparent background)
- hero-*.jpg                 (homepage hero photography, optional)
- projects/*.jpg              (real project photos, one set per project.html card)
- products/*.jpg              (product photography, one per products.html card)
- team/*.jpg                  (optional, for about.html)

The site currently renders without any images (CSS gradient + icon placeholders),
so it is safe to launch as-is and swap files in later. See ../CONTENT-TODO.md.
